import { HomeScreen } from "@/features/home/screens/home-screen";

/**
 * Home tab at URL `/home`. Thin wrapper around the feature screen so
 * the Expo Router file tree stays decoupled from the feature folder.
 */
export default HomeScreen;
