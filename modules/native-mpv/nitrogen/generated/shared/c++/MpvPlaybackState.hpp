///
/// MpvPlaybackState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nativempv {

  /**
   * An enum which can be represented as a JavaScript union (MpvPlaybackState).
   */
  enum class MpvPlaybackState {
    IDLE      SWIFT_NAME(idle) = 0,
    LOADING      SWIFT_NAME(loading) = 1,
    PLAYING      SWIFT_NAME(playing) = 2,
    PAUSED      SWIFT_NAME(paused) = 3,
    ENDED      SWIFT_NAME(ended) = 4,
    ERROR      SWIFT_NAME(error) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nativempv

namespace margelo::nitro {

  // C++ MpvPlaybackState <> JS MpvPlaybackState (union)
  template <>
  struct JSIConverter<margelo::nitro::nativempv::MpvPlaybackState> final {
    static inline margelo::nitro::nativempv::MpvPlaybackState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"): return margelo::nitro::nativempv::MpvPlaybackState::IDLE;
        case hashString("loading"): return margelo::nitro::nativempv::MpvPlaybackState::LOADING;
        case hashString("playing"): return margelo::nitro::nativempv::MpvPlaybackState::PLAYING;
        case hashString("paused"): return margelo::nitro::nativempv::MpvPlaybackState::PAUSED;
        case hashString("ended"): return margelo::nitro::nativempv::MpvPlaybackState::ENDED;
        case hashString("error"): return margelo::nitro::nativempv::MpvPlaybackState::ERROR;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum MpvPlaybackState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nativempv::MpvPlaybackState arg) {
      switch (arg) {
        case margelo::nitro::nativempv::MpvPlaybackState::IDLE: return JSIConverter<std::string>::toJSI(runtime, "idle");
        case margelo::nitro::nativempv::MpvPlaybackState::LOADING: return JSIConverter<std::string>::toJSI(runtime, "loading");
        case margelo::nitro::nativempv::MpvPlaybackState::PLAYING: return JSIConverter<std::string>::toJSI(runtime, "playing");
        case margelo::nitro::nativempv::MpvPlaybackState::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::nativempv::MpvPlaybackState::ENDED: return JSIConverter<std::string>::toJSI(runtime, "ended");
        case margelo::nitro::nativempv::MpvPlaybackState::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert MpvPlaybackState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"):
        case hashString("loading"):
        case hashString("playing"):
        case hashString("paused"):
        case hashString("ended"):
        case hashString("error"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
