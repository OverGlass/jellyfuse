///
/// NativeDownloadState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::downloader {

  /**
   * An enum which can be represented as a JavaScript union (NativeDownloadState).
   */
  enum class NativeDownloadState {
    QUEUED      SWIFT_NAME(queued) = 0,
    DOWNLOADING      SWIFT_NAME(downloading) = 1,
    PAUSED      SWIFT_NAME(paused) = 2,
    DONE      SWIFT_NAME(done) = 3,
    FAILED      SWIFT_NAME(failed) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::downloader

namespace margelo::nitro {

  // C++ NativeDownloadState <> JS NativeDownloadState (union)
  template <>
  struct JSIConverter<margelo::nitro::downloader::NativeDownloadState> final {
    static inline margelo::nitro::downloader::NativeDownloadState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("queued"): return margelo::nitro::downloader::NativeDownloadState::QUEUED;
        case hashString("downloading"): return margelo::nitro::downloader::NativeDownloadState::DOWNLOADING;
        case hashString("paused"): return margelo::nitro::downloader::NativeDownloadState::PAUSED;
        case hashString("done"): return margelo::nitro::downloader::NativeDownloadState::DONE;
        case hashString("failed"): return margelo::nitro::downloader::NativeDownloadState::FAILED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum NativeDownloadState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::downloader::NativeDownloadState arg) {
      switch (arg) {
        case margelo::nitro::downloader::NativeDownloadState::QUEUED: return JSIConverter<std::string>::toJSI(runtime, "queued");
        case margelo::nitro::downloader::NativeDownloadState::DOWNLOADING: return JSIConverter<std::string>::toJSI(runtime, "downloading");
        case margelo::nitro::downloader::NativeDownloadState::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::downloader::NativeDownloadState::DONE: return JSIConverter<std::string>::toJSI(runtime, "done");
        case margelo::nitro::downloader::NativeDownloadState::FAILED: return JSIConverter<std::string>::toJSI(runtime, "failed");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert NativeDownloadState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("queued"):
        case hashString("downloading"):
        case hashString("paused"):
        case hashString("done"):
        case hashString("failed"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
