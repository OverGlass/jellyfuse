///
/// HybridDownloaderSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `DownloadOptions` to properly resolve imports.
namespace margelo::nitro::downloader { struct DownloadOptions; }
// Forward declaration of `NativeDownloadRecord` to properly resolve imports.
namespace margelo::nitro::downloader { struct NativeDownloadRecord; }
// Forward declaration of `DownloaderListener` to properly resolve imports.
namespace margelo::nitro::downloader { struct DownloaderListener; }
// Forward declaration of `NativeDownloadState` to properly resolve imports.
namespace margelo::nitro::downloader { enum class NativeDownloadState; }

#include <string>
#include "DownloadOptions.hpp"
#include "NativeDownloadRecord.hpp"
#include <vector>
#include "DownloaderListener.hpp"
#include <functional>
#include "NativeDownloadState.hpp"

namespace margelo::nitro::downloader {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Downloader`
   * Inherit this class to create instances of `HybridDownloaderSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridDownloader: public HybridDownloaderSpec {
   * public:
   *   HybridDownloader(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridDownloaderSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridDownloaderSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridDownloaderSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string enqueue(const DownloadOptions& options) = 0;
      virtual void pause(const std::string& id) = 0;
      virtual void resume(const std::string& id) = 0;
      virtual void cancel(const std::string& id) = 0;
      virtual void delete(const std::string& id) = 0;
      virtual void rebaseAllPaths(const std::string& newDocumentDirectory) = 0;
      virtual void clearAll() = 0;
      virtual std::vector<NativeDownloadRecord> list() = 0;
      virtual DownloaderListener addProgressListener(const std::function<void(const std::string& /* id */, double /* bytesDownloaded */, double /* bytesTotal */)>& onProgress) = 0;
      virtual DownloaderListener addStateChangeListener(const std::function<void(const std::string& /* id */, NativeDownloadState /* state */)>& onStateChange) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Downloader";
  };

} // namespace margelo::nitro::downloader
